apiVersion: v1
kind: Service
metadata:
  name: report-service
  labels:
    app.kubernetes.io/name: report-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: report-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
